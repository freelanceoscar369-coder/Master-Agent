# prompts/

Reusable prompt templates for the Planner, Intent Layer, and any other
module that calls a model provider — kept as versioned text/markdown
files here rather than hardcoded in Python, so prompts can be iterated
on and reviewed like any other project asset.
