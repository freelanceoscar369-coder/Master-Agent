# scripts/

Founder/dev-ops scripts that aren't part of the installable package —
setup scripts, one-off migration scripts, local automation. Distinct
from src/master_agent/, which is the shipped engine code.
