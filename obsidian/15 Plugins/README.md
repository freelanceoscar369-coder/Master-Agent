# 15 Plugins

Ideas, specs, and notes for future plugins/capabilities — distinct from
the actual plugin code in src/master_agent/plugins/ and from the
external-plugin drop folder plugins/ at the repo root. This is where a
plugin idea lives before it's real.
