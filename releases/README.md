# releases/

Built release artifacts (packaged builds of the desktop app, once that
exists) — output of a build step, not source. Kept out of git; see
.gitignore. Distinct from docs/adr/ and docs/MISSION_BRIEF_*.md, which
document decisions and milestones, not ship binaries.
