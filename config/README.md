# config/

Local configuration and environment files (e.g. a future .env or
config.local.toml overriding the defaults in src/master_agent/config.py).
Kept out of git by default — this is where machine-specific or secret
config belongs, never committed.
